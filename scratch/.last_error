ticket-0022:
  • Code Review → 退出码 1
  发现了 3 个问题（含 1 个坏味道 + 1 个兼容性缺陷 + 1 个 scope creep），以下是具体问题和修改建议。

---

## 问题 1：`no-undef: "off"` 作用域过宽（坏味道）

**问题**：`eslint.config.mjs` 中 `no-undef: "off"` 没有 `files` 过滤器，它对**所有文件**生效，包括 `scripts/**/*.mjs`。导致：
- 下面 `.mjs` 配置块声明的 `globals: { process, console, ... }` 形同虚设——永远不会被用到
- `.mjs` 脚本文件失去了未定义变量引用检查

**修复**：给全局 rules 块加上 `files` 过滤器，只对 TS 文件关闭 `no-undef`：

```js
// 修改前
{
  rules: {
    "no-undef": "off",
    // ...
  },
},

// 修改后
{
  files: ["src/**/*.ts", "tests/**/*.ts", "scripts/**/*.ts"]