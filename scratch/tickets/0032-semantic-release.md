# ticket-0032: Semantic Release

## 目标

基于 conventional commits 自动版本号 + 发布到 npm。

## 任务

- [ ] 安装 `semantic-release` + GitHub + npm plugins
- [ ] `.releaserc` 配置
- [ ] 更新 publish.yml 使用 semantic-release
- [ ] 验证 dry-run

## 产出

- push to main 自动检测版本 bump
- 自动发布到 npm
- 自动创建 GitHub Release
